spring:
  cloud:
    kubernetes:
      config:
        enabled: true
        sources:
          - namespace: default
            name: market-place-iam
      reload:
        enabled: true
        mode: event
        strategy: refresh